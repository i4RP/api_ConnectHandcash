import Run from 'run-sdk';
declare class CustomBlockchain extends Run.plugins.WhatsOnChain {
    broadcast(rawTx: string): string;
    fetch(txid: string): Promise<any>;
}
export default CustomBlockchain;
