import { RequestParams } from '../types/http';
import HandCashConnectApiError from '../api/handcash_connect_api_error';
type Params = {
    apiKey: string;
    cloudName: string;
    uploadPreset?: string;
};
export default class CloudinaryImageService {
    static baseApiEndpoint: string;
    apiKey: string;
    cloudName: string;
    uploadPreset: string;
    constructor({ apiKey, cloudName, uploadPreset }: Params);
    uploadImage(filePath: string): Promise<{
        imageUrl: string;
    }>;
    static handleRequest<T>(requestParameters: RequestParams, stack: string | undefined): Promise<T>;
    static handleApiError(result: {
        stack?: string;
        request: RequestParams;
        response?: Response;
    }): Promise<HandCashConnectApiError>;
}
export {};
