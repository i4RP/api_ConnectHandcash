import { CollectionDefinition } from '../types/items';
export default class JsonCollectionMetadataLoader {
    loadFromData(rawData: string): Promise<CollectionDefinition>;
    private static loadItemFromRawItemData;
}
